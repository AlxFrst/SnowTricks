{% extends 'base.html.twig' %}

{% block title %}SnowTricks - Ajouter un trick
{% endblock %}

{% block body %}
	<div class="flex justify-center mt-10">
		<div class="w-full max-w-4xl p-5 shadow-lg bg-white rounded-lg">
			<h1 class="text-2xl font-bold mb-5">Ajouter un trick</h1>
			{{ include('trick/_form.html.twig') }}
			<a class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg text-lg mt-5 inline-block" href="{{ path('app_trick_index') }}">Retour à la liste</a>
		</div>
	</div>
{% endblock %}



{% block javascripts %}
	{{ parent() }}
	{{ encore_entry_script_tags('trickAdd') }}
{% endblock %}
