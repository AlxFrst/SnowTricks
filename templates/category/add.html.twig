{% extends 'base.html.twig' %}

{% block title %}Add a Category
{% endblock %}

{% block body %}
	<div class="flex justify-center items-center min-h-screen bg-gray-100">
		<div class="w-full max-w-2xl mx-auto p-6 bg-white rounded-lg shadow-md">
			<h1 class="text-3xl font-semibold text-gray-800">Ajouter une catégorie</h1>
			{% for message in app.flashes('success') %}
				<div class="mt-4 p-3 bg-green-200 text-green-800 rounded">
					{{ message }}
				</div>
			{% endfor %}
			<div class="mt-6">
				{{ form(form, {'attr': {'class': 'space-y-6'}}) }}
				</div>
			</div>
		</div>
	{% endblock %}

